# -----------------------------
# OpenAI
# -----------------------------
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
# Modèle (par défaut: gpt-5.2)
OPENAI_MODEL=gpt-5.2
# GPT-5.2: température possible seulement avec reasoning effort = none
OPENAI_REASONING_EFFORT=none
OPENAI_TEMPERATURE=0.3
# Limite de sortie (Responses API)
OPENAI_MAX_OUTPUT_TOKENS=900

# -----------------------------
# Sécurité admin (optionnel)
# -----------------------------
# Token d'accès au dashboard /admin (si vide, pas de protection)
ADMIN_DASHBOARD_TOKEN=changez-moi
# Alias legacy (facultatif) :
# ADMIN_API_TOKEN=changez-moi

# Historique utilisateur (optionnel)
USER_HISTORY_TOKEN=

# Fuseau horaire (utile pour logs)
TZ=Europe/Paris

# -----------------------------
# BT (assistant interne) - recommandé
# -----------------------------
# Token obligatoire pour appeler /bt/* (header: X-BT-Token)
BT_API_TOKEN=changez-moi

# Profil BT (YAML) chargé au démarrage
BT_PROFILE_PATH=/app/config/bt_profile.yaml

# Limites de sortie BT (optionnel)
BT_MAX_OUTPUT_TOKENS=400
BT_MAX_REPLY_CHARS=500

# -----------------------------
# EZVIZ (OPTIONNEL) - paramètres de connexion
# -----------------------------
# IMPORTANT : Render n'a généralement pas accès au réseau local de la caméra.
# Pour un accès live fiable, il faut un agent "edge" sur place OU exposer RTSP (déconseillé).

# Mode conseillé: rtsp
EZVIZ_MODE=rtsp

# Exemple RTSP C6N (voir doc EZVIZ): rtsp://admin:CODE@IP:554/ch1/main
# EZVIZ_RTSP_URLS peut contenir une liste séparée par des virgules.
EZVIZ_RTSP_URLS=
